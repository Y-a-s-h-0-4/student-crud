{
  "name": "student-crud",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "jest --runInBand",
    "build": "echo \"Build step completed\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.3"
  },
  "jest-junit": {
    "outputDirectory": "test-results",
    "outputName": "results.xml"
  }
}
