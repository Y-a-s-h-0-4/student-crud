{
  "name": "student-crud",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "test": "mocha __tests__/api.test.js --reporter mocha-junit-reporter",
  "build": "echo Build Successful"
},
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^6.2.1",
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "mocha": "^11.7.5",
    "supertest": "^6.3.4",
    "mocha-junit-reporter": "^2.0.0"
  },

  "mocha-junit-reporter": {
    "mochaFile": "test-results/results.xml"
  }

}
